{
  "setup_file": "./setup.py",
  "save_main_session": true,
  "datasets": [
    "stackoverflow.com"
  ],
  "pipeline": {
    "input_dir": "gs://so-post-data/datasets/",
    "output_dir": "gs://so-post-data/output/",
    "pipeline_options": {
      "runner": "DataflowRunner",
      "project": "surprisal-se",
      "region": "us-east1",
      "temp_location": "gs://so-post-data/temp/",
      "staging_location": "gs://so-post-data/staging/",
      "job_name": "preprocessing-pipeline"
    }
  }
}
